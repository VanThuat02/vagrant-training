# Ẩn thông tin phiên bản Apache
ServerTokens Prod
ServerSignature Off

# Vô hiệu hóa HTTP Trace
TraceEnable Off

# Ngăn truy cập file .htaccess, .htpasswd
<FilesMatch "^\.ht">
    Require all denied
</FilesMatch>
